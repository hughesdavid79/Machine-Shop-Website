services:
  - type: web
    name: machine-shop-website-backend
    env: node
    buildCommand: |
      cd server
      npm install
      npm ci
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
    domains:
      - api.rpomachineshop.com

  - type: static
    name: machine-shop-website-frontend
    buildCommand: |
      npm install
      npm run build
    publishDir: dist
    envVars:
      - key: VITE_API_URL
        value: https://api.rpomachineshop.com
    domains:
      - rpomachineshop.com
      - www.rpomachineshop.com
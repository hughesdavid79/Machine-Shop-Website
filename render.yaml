services:
  - type: web
    name: machine-shop-website-backend
    env: node
    buildCommand: |
      cd server
      npm install
      npm ci
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
    healthCheckPath: /api/health